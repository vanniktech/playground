import app.playground.frontend.Amount;

CREATE TABLE foo (
  id TEXT NOT NULL PRIMARY KEY,
  amount INTEGER AS Amount NOT NULL
);

upsert:
INSERT OR REPLACE INTO foo
VALUES ?
;

deleteAll:
DELETE FROM foo
;

sum:
SELECT
  SUM(amount)
  FROM foo
;
