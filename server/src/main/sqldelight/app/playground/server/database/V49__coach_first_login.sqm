ALTER TABLE coach ADD COLUMN first_login TIMESTAMPTZ;
UPDATE coach SET first_login = created; -- This is not entirely true but we don't necessarily care about this that much.

ALTER TABLE coach ADD COLUMN first_login_reminder TIMESTAMPTZ;
